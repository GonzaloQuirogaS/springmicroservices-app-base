server:
  port: 8761

spring:
  application:
    name: msvc-eureka
  config:
    #Temporal hasta configurar config server
    import: optional:configserver:http://localhost:8888

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: http://localhost:${server.port}/eureka